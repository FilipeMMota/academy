package com.ctw.workstation.databases;

import jakarta.enterprise.context.ApplicationScoped;

@Deprecated
@ApplicationScoped
public final class RackDatabase {
    /*private final Map<String, Rack> entity;

    public RackDatabase() {
        this.entity = new HashMap<>();
    }

    public void addEntity(Rack newEntity) {
        entity.put(newEntity.getId(), newEntity);
    }

    public void deleteEntity(String id) {
        entity.remove(id);
    }

    public Rack getEntity(String id) {
        return entity.get(id);
    }

    public Map<String, Rack> getAllEntities() {
        return entity;
    }*/
}
